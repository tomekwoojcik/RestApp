import Data from "../../context/localhostContext"

const RequireAuth = () => {
    const data: any = new Data("user");
    console.log(data);
    return (
        <div>
            
        </div>
    )
}

export default RequireAuth