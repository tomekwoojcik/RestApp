import { Button } from "@mui/material";
import { FC } from "react";

interface stateModel =  {
    
}

const CancelLeaveButton : FC = (state) => <Button variant="contained">{"Cancel Leave"}</Button>

export default CancelLeaveButton