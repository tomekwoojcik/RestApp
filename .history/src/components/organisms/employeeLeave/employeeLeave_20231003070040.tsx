import { SupervisorProvider } from "../../context/supervisorContext";

const EmployeeLeave = () => {
    return (
        <SupervisorProvider>
                
        </SupervisorProvider>
  );
};

export default EmployeeLeave;
