import { FC, useContext } from "react";
import { SupervisorContext } from "../../context/supervisorContext";

const WorkersList: FC = () => {
  const {  } = useContext(SupervisorContext);
  return <div></div>;
};

export default WorkersList;
